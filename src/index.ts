/**
 * Checks if a number is strictly equal to 7.
 * @param n - The number to check
 * @returns true if n is 7, false otherwise
 */
export function isSeven(n: number | string): boolean {
    return n === 7 || n === '7' || n === "7";
}